//#region lib/types/index.js
/**
* Host loader entry for the browser-only SillyTavern regex-script panel plugin.
*/
/** Provides no host-side behavior; script storage lives in dsh-st-regex behind st-api. */
function apply() {}
//#endregion
export { apply };
