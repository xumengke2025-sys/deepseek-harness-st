/** Host loader entry for the browser-only SillyTavern character panel plugin. */

/** Provides no host-side behavior; data services live in the `dsh-st-*` packages. */
export function apply(): void {}
